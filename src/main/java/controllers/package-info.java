/**
 * Contains the controller classes which define the http route methods for each model.
 * <p>
 * The controllers for Assessment, AssessmentType, Category, and Grade use Handler methods
 * to define the functionality for the routes in the App class. OpenApi (swagger) annotations
 * were added to each method for the route documentations.
 *
 * @since 1.0
 * @see java.awt
 */
package controllers;